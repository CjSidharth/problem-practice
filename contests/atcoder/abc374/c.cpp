#include <bits/stdc++.h>
using namespace std;
int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int n;
    vector<int> k;
    for(int i = 0; i < n; i++){
        int x;
        cin >> x;
        k.push_back(x);
    }
    return 0;
}