#include <bits/stdc++.h>
using namespace std;
int main()
{
    long long n,m;
    vector<bool> num;
    long long ans = 0;
    for(int i = 0,j=1; i < m,j<=n; i++)
    {
        long long x;
        string ver;
        cin >> x >> ver;
        if(num[x-1] == false)
    }
}