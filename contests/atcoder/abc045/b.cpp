#include <bits/stdc++.h>
using namespace std;
int main()
{
    pair<string,int> s1,s2,s3;
    cin >> s1.first >> s2.first >> s3.first;
    bool run = true;
    s1.second = 0;
    s2.second = 0;
    s3.second = 0;
    int turn = 0;
    while(run)
    {
        if(turn == 0 && s1.second != s1.first.size())
        {

        }

    }
}