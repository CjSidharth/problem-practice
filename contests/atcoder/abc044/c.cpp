#include <bits/stdc++.h>
using namespace std;


int main(){
    int n,a;
    cin >> n >> a;
    vector<int> num(n);
    for(auto & x : num) cin >> x;
    for(int i = 0 ; i < n; i++){
        
    }

}