#include <bits/stdc++.h>
using namespace std;
int main(){
    long long n, q;
    map<long long, map<long long,long long>> mp;
    cin >> n >> q;
    for(long long i = 0; i < q; i++){
        int o;
        cin >> o;
        int a,b;
        if(o == 1){
            cin >> a >> b;
        }
        else if(o == 2){
            cin >> a >> b;
        }
        else{
            cin >> a;
        }
    }
}